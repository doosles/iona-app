# Tasks: A Physical Button That Summons Help

**Input**: `/specs/005-physical-help-button/` — spec.md, plan.md, checklists/safety.md
**Feature**: Flic 2 physical button → the existing reactive help sequence, via a native Capacitor plugin.

**Status reconciled 2026-07-03** — checkbox state below matches verified on-disk + on-device truth (see the reconcile notes on T003 / T030 / T035 and the closing Notes). Phases 1–2 + press-time recovery (T035) + the service-test **backend** (T027 `/service-test` + T028 EventLog types) are built + verified; the **T012 pairing UI** is built + on-device-render-verified and the **T033 chime asset** bundled (2026-07-03); PRE-1 mockup built (owner commit pending). Phases 3–6: 7 done (T012, T013, T017, T025, T027, T028, T029), 3 partial (T026, T030, T033), 12 not-started.

## Format & legend

`[ID] [P?] [Repo] [Story?] Description (path) — ✓ push-gate · DoD on-device`

- **[P]**: can run in parallel (different files, no dependency)
- **[Repo]**: `[NATIVE]` Android/Java · `[APP]` www/* JS+HTML+CSS · `[BE]` howsu backend (cleanup only)
- **[Story]**: `[US1]` core summon · `[US2]` act-now/never-stack (safety) · `[US3]` durable pairing · `[US4]` pairing flow · `[US5]` status/remove/gesture · `[US6]` self-test / service test (reassurance + no-silent-death)
- **Push-gates**: `node --check www/app.js` (JS) · `./gradlew assembleDebug` clean (native). **"Verified" = on the physical Pixel + physical Flic** — never build/deploy alone.

---

## Phase 0 — Prerequisites (blocking; not engineering tasks)

- [~] PRE-1 [Owner · iona-app] **Pairing mockup located + updated** — `iona_pair_button_flow_four_states.html` (repo root; Iona night tokens, orb idiom, amber confirm-press). ✅ the missing Android **"Pair & Connect"** system-dialog line added at step 2 (2 Jul); ✅ a **fifth state added — the button-test feedback** ("working" = teal orb + ping rings + chime/wave motif; honest "couldn't confirm" sibling — US6). **Remaining:** commit to the repo (owner). **NB — iona-app IS a git repo that uses commits** (recent history e.g. "UX: pause before Iona reply card…"), unlike the howsu workspace's no-commit norm — so committing the mockup here is expected, not a rule-break. Unblocks the pairing-flow UI (US4) **and** the test-confirmation UI (US6, T025).
- [x] PRE-2 [CC · bring-up] **Pinned `flic2lib-android` JitPack tag = `2.0.1`** (latest stable, GitHub 50ButtonsEach, Dec 2025) in `android/app/build.gradle`. Resolved + compiled on device 2026-07-02.

---

## Phase 1 — Native foundation & build chain (blocks ALL native work)

- [x] T001 [NATIVE] Add JitPack to `android/build.gradle` `allprojects { repositories { … } }` — `maven { url 'https://jitpack.io' }` (there is no `dependencyResolutionManagement` in settings.gradle, so it belongs here). — ✓ `./gradlew help` resolves · DoD: repo added.
- [x] T002 [NATIVE] Add the dependency to `android/app/build.gradle` `dependencies { }` — `implementation 'com.github.50ButtonsEach:flic2lib-android:<PRE-2 tag>'`. (depends PRE-2, T001) — ✓ `./gradlew assembleDebug` resolves the artifact.
- [x] T003 [NATIVE] Edit `android/app/src/main/AndroidManifest.xml`: add `ACCESS_FINE_LOCATION`, `BLUETOOTH_SCAN`, `BLUETOOTH_CONNECT`, `FOREGROUND_SERVICE_CONNECTED_DEVICE`, **`RECEIVE_BOOT_COMPLETED`** (reboot survival, T023), **`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`** (OEM survival, T013); declare `<service android:name=".FlicListeningService" android:foregroundServiceType="connectedDevice" android:exported="false"/>` and **`<receiver android:name=".BootReceiver" android:exported="true">`** with a `BOOT_COMPLETED` + `LOCKED_BOOT_COMPLETED` intent-filter (T023); set `<application android:name=".IonaApplication">`. — ✓ manifest merges · DoD: build installs on Pixel. **⚠ RECONCILE 2026-07-03 — PARTIAL:** the boot-survival declarations (`RECEIVE_BOOT_COMPLETED` + `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` perms and the `<receiver .BootReceiver>` element) are **NOT yet in the manifest** — only a placeholder comment; they land with T013/T023. Core done + on device: BLE/location perms, `FOREGROUND_SERVICE_CONNECTED_DEVICE`, `USE_FULL_SCREEN_INTENT`, `POST_NOTIFICATIONS`, `<service .FlicListeningService>`, `<application .IonaApplication>`. **✅ RECONCILED 2026-07-04** — the boot-survival declarations now landed: `RECEIVE_BOOT_COMPLETED` + `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` perms and `<receiver .BootReceiver android:exported="true" android:directBootAware="true">` with a `BOOT_COMPLETED` + `LOCKED_BOOT_COMPLETED` intent-filter. `assembleDebug` BUILD SUCCESSFUL; on-device install rides T016.
- [x] T004 [NATIVE] Create `com/iona/app/IonaApplication.java` (`extends Application`) — initialise the Flic manager **once** in `onCreate` per SDK docs. (depends T002, T003) — ✓ `assembleDebug` · DoD: app launches, manager init logged.
- [x] T005 [NATIVE] Create `com/iona/app/FlicPlugin.java` (`extends Plugin`, `static instance`, `load()` sets it — mirror `ZeroCallPlugin`). Commands: `startScan`, `stopScan`, `getButtons`, `removeButton`, `getPairingState`, **`isIgnoringBatteryOptimizations`**, **`requestBatteryExemption`** (T013). Events via `notifyListeners`: `buttonSummon`, **`buttonSelfTest`** (double-tap, T025), `buttonFound`, `pairingComplete`, `connectionChanged`. Register with `registerPlugin(FlicPlugin.class)` in `MainActivity.java:14`. (depends T004) — ✓ `assembleDebug` · DoD: `Capacitor.Plugins.Flic` resolves in the WebView.
- [x] T006 [NATIVE] Create `com/iona/app/FlicListeningService.java` — foreground service (`connectedDevice` type), persistent notification **"Iona is here for you."**; on start, **reconnect-on-launch**: `getButtons()` → connect/re-attach listeners at the right moment (the SDK gotcha). (depends T005) — ✓ `assembleDebug` · DoD on Pixel: service runs, "Iona is here for you." notification visible. **Service-gating 2026-07-04 (FR-023a):** the FGS + notification exist ONLY while a button is paired — started on pairing, torn down on last removal (T018), and armed from launch / boot / `START_STICKY` only when a paired button exists (device-protected `has_button` hint gates the pre-unlock nudge). No button → no service → no notification → no battery cost. Paired-button always-armed guarantee unchanged (re-verify via 2b).

**Checkpoint**: native scaffold builds, installs, launches; plugin reachable; listening service + notification live on the Pixel.

---

## Phase 2 — Summon MVP (US1 core + US2 safety) 🎯 SAFETY MVP — prove first

**Goal**: a (dev-paired) button press starts the EXISTING help sequence, stale presses are dropped, duplicates absorbed. This is the load-bearing reactive path (Constitution I.4) — nothing cosmetic precedes it.

- [x] T007 [NATIVE] [US2] **Stale-press guard** in `FlicPlugin.routePress` — **REVISED on device (2026-07-02):** drop on the SDK's **`wasQueued`** flag (a press buffered while out of range, delivered on reconnect), before `notifyListeners('buttonSummon')`; a live press is honoured. **No numeric-age threshold** — flic2lib's event timestamp is a button-relative clock, not comparable to phone time (an age calc gave ~56 000 yr / ~4 days). Matches FR-007's literal case. — ✓ `assembleDebug` · verified via T011/SC-002.
- [x] T008 [NATIVE] [US1] **Gesture filter** in `FlicPlugin` — routing **depends on the chosen summon gesture** (FR-026a):
  **(a) summon = short-press (DEFAULT — corrected 2026-07-04)** → **single, double, AND hold ALL → `buttonSummon`**; **`buttonSelfTest` is NOT emitted from the button** — a tremor the SDK classifies as a double-click MUST still summon (the OQ1-B safety edge). Short-press users test via the in-app "Test service" control only (T029).
  **(b) summon = hold (opt-in)** → `hold` → `buttonSummon` (carrying gesture type + press age); `double-tap` → `buttonSelfTest` (a distinct event, **never** `buttonSummon` — a self-test must never enter the help sequence, T025); single-click ignored.
  (depends T005) — ✓ `assembleDebug`. **✅ DEFAULT FLIPPED 2026-07-04** — `FlicPlugin.summonGesture` now defaults to `"short"` (single-press, telecare-familiar), not `"hold"`. The gesture filter itself is UNCHANGED — this just flips which branch is default (per the fence). `compileDebugJavaWithJavac` clean.
- [x] T009 [APP] [US1] In `www/app.js`, register the `buttonSummon` listener at launch → call **`_startHelpSequence('physical_button')`** (new trigger label for provenance; path identical to the in-app control — inherits the cancel window + `escalation_state` idle-guard). Re-register the listener on app `resume`. (depends T005) — ✓ `node --check www/app.js`.
- [x] T010 [APP] [US1] **On-device (Pixel + Flic):** dev-pair a button, phone on + in range, press-and-hold → the existing help sequence runs **end-to-end, identical to the in-app help control** (cancel window → reaching people). (depends T009) — DoD: SC-001 observed on device.
- [x] T011 [NATIVE+APP] [US2] **On-device safety proof — PASS 2026-07-02:** (a) Bluetooth off → press → wait >15 s → reconnect → **no** late sequence, 3 queued presses dropped, 0 leak [SC-002]; (b) with a sequence **already running**, press again (2nd + 3rd) → **absorbed**, live one undisturbed [SC-003]. **FINDING:** SC-003 needed a fix — the `escalation_state` guard only covered the committed phase; added an `_alarmFlowActive` in-flight guard in `_startHelpSequence` covering the cancel window (absorption is **guard-enforced, not merely inherited**). (depends T007, T010) — DoD: both observed; safety.md CHK004/CHK008 pass.

- [x] T034 [NATIVE+APP] [US1] *(added 2 Jul — on-device finding: the button MUST work while the app is CLOSED)* **Works-while-closed via full-screen intent.** The FGS keeps the process+BLE alive, but the summon logic is in the WebView (destroyed on close) — so a closed-app press fired `buttonSummon` into a dead WebView. Fix: on a summon while not foreground, `FlicPlugin` raises a **full-screen-intent** notification → `MainActivity` (`flic_summon`) → **`_startHelpSequence`**, waking the phone **over the lock**. Denied-FSI floor = a high-importance **sounding** notification (never silent); `canUseFullScreenIntent()`/grant-redirect commands added for Phase-3 pairing (A14/Play deferred). The launch summon is a **one-shot** consumed exactly once (a retained event LOOPED — repeat sequences/calls; fixed). Orb-overlay z-race fixed (`showOrb` guarded on `_alarmFlowActive`). **PASS 2026-07-02:** closed + locked → one clean sequence over the lock; normal launch fires no phantom summon. (FR-033/FR-033a) — DoD: observed on Pixel + Flic.

- [x] T035 [BE+APP] [US2] *(added 2026-07-03 — "a help press must never be silent")* **Press-time recovery.** The never-stack absorb (T011/SC-003) could **brick the button** if a terminal/outcome FCM was lost — the flag stuck `active` and every repeat press swallowed. Fix = **heal-on-next-press** via a purpose-built backend liveness read with **hard bias-to-summon** (not a timer). **BE (`reply_to_airtable_webhook.py`, additive only):** `ESCALATION_IN_FLIGHT` + `ESCALATION_LIVE_TTL_SECONDS = 45*60` (self-expiring → a missed clear can only bias to summon, never brick); `_mark_escalation_in_flight` / `_escalation_is_live` / `_run_escalation_tracked` wrapping all 5 `run_escalation` sites (ladder untouched); `_send_escalation_outcome_once` pops the rec the instant the outcome fires; new read-only `GET /pwa-escalation-live?rec=` → `{"live": bool}` + `[LIVENESS]` log + CORS. **APP (`www/app.js`):** `_escalationConfirmedLive()` (local device-dial → 45-min stale fast-path → 1200 ms backend read; TRUE only on positive live), `_summonEvaluating` re-entrancy flag, `_startHelpSequence` guard rewritten (absorb ONLY when active+confirmed-live OR a live cancel-window countdown; else heal + summon THIS press). Timing unified at **45 min** across backend TTL / app stale / cold-init / `ALARM_ESCALATION_TIMEOUT_MS`. (depends T011) — ✓ `py_compile` (ff"=0) + `node --check` · **DoD on Pixel 4a (2026-07-03, servers up): B1 stuck→summons, B2 flurry→exactly-one, B3 live→absorbs — all PASS.** Captain (Claude-web) review outstanding.

**Checkpoint**: the button reliably starts the real help sequence, both safety rules hold on hardware, **it works with the app closed + locked, and a stuck flag heals on the next press.**

---

## Phase 3 — US4 Pairing flow (calm one-time setup) — Priority P2

**Depends on PRE-1 (mockup).**

- [x] T012 [APP] [US4] Build the four-state pairing flow container (intro → searching → confirm-press → ready) in `www/index.html` + `www/style.css`, bound to **live Iona night tokens + orb idiom** (teal presence; amber on confirm-press) — **NOT** mockup hex; copy from the mockup — **copy is PROVISIONAL (owner, 2026-07-03: not locked, but fine to build against for this build; may be refined later)**, within the banned-terms list. **Includes the step-2 "Pair & Connect" line** (now in the mockup). (depends PRE-1) — ✓ `node --check`/file loads · DoD: renders on Pixel. **✅ BUILT (inert) 2026-07-03** — `#pairing-overlay` in index.html (6 states: intro / searching / confirm / ready + test-working / test-failed; one `.is-active`, default intro) + token-bound CSS in style.css (live night tokens, day auto-flips, orb idiom reused, NOT mockup hex). Validated: CSS braces 312/312, tags balanced, exactly 1 default state. **on-device render VERIFIED 2026-07-03** (Pixel 4a — intro + searching states via a TEMPORARY dev-panel "Pairing ▸" preview: opening auto-hides the dev panel, tap-anywhere steps through all 6, ✕ closes; deep-sea card / teal + amber orbs / ripple rings / color-mix status pills / bright-teal CTA all render correctly). Still **NOT wired** (no trigger / real state machine — that's T013); the preview harness is temporary — removed with the Flic dev panel at Phase 3. Secondary-grey may want a taste tweak — note `--text-soft` is self-referential/broken on the night `:root` (pre-existing; avoided here).
- [x] T013 [APP] [US4] Wire the flow to `FlicPlugin` in `www/app.js`: at step 2 request `FINE_LOCATION`+`BLUETOOTH_SCAN`/`CONNECT` with the **calm one-line pre-explanation before the OS prompt**; `startScan` → `buttonFound` → confirm-press (step 3 = the "right button" check) → `pairingComplete` → ready; include the **"tap Pair & Connect" line at step 2→3**. **After `pairingComplete`, request the battery-optimization exemption** — gate on `isIgnoringBatteryOptimizations`, then `requestBatteryExemption` with **one calm line ("so Iona stays awake to hear your button") before the system dialog**; skip gracefully if already exempt or unsupported. (depends T005, T012) — ✓ `node --check`. **✅ BUILT + on-device verified to the scan boundary 2026-07-03.** `_wirePairingFlow()` (app.js) drives `#pairing-overlay` via the real FlicPlugin: intro → [Pair my button] → calm pre-explanation note + `startScan` (self-requests BLE/location perms) → searching → `buttonFound` → confirm (status → "Connecting…") → `pairingComplete` → ready → battery-opt (`isIgnoringBatteryOptimizations` → `requestBatteryExemption`, "so Iona stays awake…", skips if exempt/unsupported); any deny/failure returns to intro with a calm note; ✕ / Not now stop the scan. Real entry: **Settings → Service → "Pair a help button"** (`#btn-pair-button`) + dev-panel **"Pair flow ▸"** (both → `window._openPairingFlow()`); overlay z-index 120 (above Settings z 100), opening closes Settings + hides the dev panel; inert T012 preview stepper retired. **On-device (Pixel 4a):** intro → "Pair my button" → pre-explanation toast + Searching + live `startScan` confirmed (BLE perms already granted, no prompt). **REMAINING = T014** (owner + physical Flic): hold-to-discover → connect → paired → the battery-opt dialog (SC-005).
- [ ] T014 [APP] [US4] **On-device:** complete introduce→find→confirm-press→ready on the Pixel, **including the OS permission prompt and Android's "Pair & Connect" dialog**, with no confusing or alarming moment. (depends T013) — DoD: SC-005 observed.

---

## Phase 4 — US3 Durable pairing (paired once, paired for good) — Priority P1

- [x] T015 [NATIVE] [US3] Confirm pairing persists in the SDK's internal storage and harden **reconnect-on-launch** (from T006) — re-attach at the correct lifecycle moment so presses never silently stop firing. — ✓ `assembleDebug`. **✅ DONE 2026-07-04.** Pairing persistence confirmed (SDK-owned; `Flic2Manager.initAndGetInstance` in `IonaApplication.onCreate` → `getButtons()` returns paired buttons on any process start, incl. a boot-woken one with no Activity). Reconnect hardened + made Activity-independent: the press router/listener is now a **single static shared object**; re-attach runs in `FlicListeningService.onStartCommand` (right after `startForeground`) via `FlicPlugin.reconnectHeadless(applicationContext)` — so it fires on app launch, cold boot (BootReceiver, T023), AND `START_STICKY` restart, even when `FlicPlugin.instance==null`. Foreground path byte-for-byte preserved (JS events via null-safe `deliverToJs`; the summon FSI still fires when `!appForeground`); re-attach is remove-then-add of the one shared listener, so it can never double-fire. `:app:compileDebugJavaWithJavac` clean (forced rerun). On-device (kill+relaunch / reboot) rides T016.
- [ ] T023 [NATIVE] [US3] *(added 2 Jul — closes the reboot gap)* Create `com/iona/app/BootReceiver.java` (`extends BroadcastReceiver`; manifest-registered in T003 for `BOOT_COMPLETED` + `LOCKED_BOOT_COMPLETED`) → on boot, start `FlicListeningService` **as a foreground service** so the button is armed after a **cold reboot with no app launch**. SDK storage persists only the *pairing*, not a running listener — without this, SC-004's reboot leg silently fails. Mind the **Android-14 boot-time FGS-start rules** for `connectedDevice` (BOOT_COMPLETED is an allowed start reason, but the declared type + BLE perms must already be granted from pairing). Verify the reboot path on the **test Pixel (Android 13) now**; **Android-14+ boot-FGS behaviour is DEFERRED until 14+ hardware (S22) is available**. (depends T006, T015) — ✓ `assembleDebug` · DoD on Pixel: reboot → **without opening the app**, a press summons. **✅ DONE 2026-07-04** — `BootReceiver.java` created (`extends BroadcastReceiver`; handles both `BOOT_COMPLETED` + `LOCKED_BOOT_COMPLETED`) → `ContextCompat.startForegroundService(FlicListeningService)`, reusing the T006 FGS (no second service); best-effort try/catch so a refused pre-unlock start never crashes the boot broadcast (the later `BOOT_COMPLETED` / next launch re-arms). Paired with the T015 headless reconnect, a boot re-arm attaches the listener with no app launch. `assembleDebug` BUILD SUCCESSFUL. On-device reboot proof rides T016.
- [x] T016 [NATIVE+APP] [US3] **On-device:** pair → kill+relaunch app → **reboot phone (rides T023 — no app launch)** → change the button battery; after **each**, a press still summons with **0 re-pairings**. (depends T015, T023) — DoD: SC-004 observed (all three); safety.md CHK012 pass. **Build ready 2026-07-04** — T003/T015/T023 compile-verified (`assembleDebug` clean); run-sheet delivered. **✅ ON-DEVICE PASS 2026-07-04 (SC-004/CHK012):** kill+relaunch, reboot (no app launch — BootReceiver arm + unlock nudge + 120s boot-window press delivered + a full reactive bridge, no crash), battery swap (0 re-pairing) — all summon. Static-router refactor cleared. Two reboot-window crashes caught + fixed on-device (directBoot; `getInstance()`-throws-before-init). See `HANDOFF_claude-web_2026-07-04_sitting-complete.md`.
- [ ] T024 [NATIVE+APP] [US3] *(added 2 Jul — closes the OEM silent-death gap)* **On-device OEM-survival proof (Pixel, now):** with the battery-opt exemption granted (T013), background → **swipe from recents** → leave in **Doze overnight** → a press still summons. **The aggressive-OEM gate (Samsung S22) is DEFERRED until that hardware is available** (not currently owned — cf. the unattended-bridge Android-14 verdict) — a follow-up, **not a ship blocker**; where an OEM kills the FGS it is documented + surfaced by the service test (T026), never silent. (depends T013, T016) — DoD: Pixel legs pass; S22 leg tracked as deferred.

---

## Phase 5 — US5 Status/removal/gesture + US6 self-test / service test — Priority P2

*The service test (T025–T029) is the reassurance + no-silent-death layer; it is cross-repo (BE endpoint + EventLog types) and MUST prove suppression on device. Low battery (T030–T032) is the other common silent-death vector.*

- [x] T017 [APP] [US5] Add the **connected-status row** to the **Service tab, directly below `#reactive-method-card`** in `www/index.html` + `www/style.css`, fed by `connectionChanged` / `getButtons`; include a **"last confirmed working: <time>"** line updated by the self-test (T025). — ✓ `node --check` · DoD: shows "connected" on Pixel. **✅ BUILT + on-device verified 2026-07-03.** `#button-status` (Service pane) rendered by `_renderButtonStatus()` from live signals: `getPairingState().connectionState===3` (READY) = honest **"Connected"**, else **"Not connected right now"** + "Make sure your button is nearby" (calm; never a false positive — Constitution I.4). "Last confirmed working" from a local `flic_last_confirmed_working` pref, stamped on `connectionChanged`→'ready' OR a passed service test; `readBattery()` → calm battery line (low = `--signal-watch`, never alarm). Actions: **Test service** (POST `/service-test` → pass: stamp + `iona_success_chime` + "Your button's working"; fail: honest "couldn't confirm") + **Remove button** (two-tap confirm → `removeButton`). Re-renders on connectionChanged/pairingComplete/batteryLevel + foreground; State A CTA → `window._openPairingFlow()`. Token-bound (method-card idiom), banned-list clean. Dev "Status B" preview toggle added; dev panel moved top-left (clears the nav + settings ✕). **On-device (Pixel 4a):** State B verified with a REAL paired-but-out-of-range button → "Not connected right now" + "Battery 100%" + Test/Remove, styling matches — acceptance #2/#3/#5 ✅. **Owner-pending:** the "Connected" half (button in range — #6) and Test-service PASS updating last-confirmed (#4 — needs servers up). Also lands the **T018** remove + **T029** in-app "Test service" cores.
- [ ] T018 [APP] [US5] Add the **remove** control → `FlicPlugin.removeButton`; after removal presses no longer summon; **removal governs future presses only — never disturbs a running sequence** (FR-020). (depends T017) — ✓ `node --check` · DoD on Pixel: removed → no summon; re-pair works (SC-006).
- [x] T019 [APP] [US5] Add the **summon-gesture choice** setting (**default single-press** — corrected 2026-07-04; hold is the opt-in) — passes the chosen gesture to `FlicPlugin` (T008); one calm line on the trade-off. **Single-press (default) means every press summons and the button-based double-tap test is off** (FR-026a) — the test lives in the in-app "Test service" control (T029). **Hold (opt-in) users keep double-tap = test.** Settings copy: single-press = "one press for help"; hold = "press and hold, if you'd prefer a button that can't be pressed by accident" — neither implies the person chose wrong or that either is unsafe. (depends T008) — ✓ `node --check` · DoD on Pixel: switching gesture is respected on the next press, **and selecting hold enables the button double-tap test**. **⏳ PARTIAL 2026-07-04 — persistence half BUILT (pre-launch safety, FR-005a):** the chosen gesture now persists natively in **device-protected** SharedPreferences (`iona_flic`/`summon_gesture`), read by `FlicPlugin.loadSummonGesture()` at every process start — in `load()` (app open) AND in `reconnectHeadless()` (boot / no-app-launch, BEFORE the listener attaches) — so the right gesture is active on the very first post-reboot press even over the lock screen. `setSummonGesture` writes it. Default when unset = `"short"`. `compileDebugJavaWithJavac` clean. **✅ SETTINGS UI BUILT 2026-07-04:** member-facing chooser added to the Service tab (`#gesture-card`, below the button-status row), reusing the feature-004 `method-*` picker markup/styling exactly (US6/US7-scoped). Two radio-style rows — **"One press for help"** (default) / **"Press and hold"** (opt-in) with the owner-settled copy, plus one calm foot line pointing to "Test service"; shown only when a button is paired. `_renderGestureChooser()` reflects the live persisted gesture on init / connectionChanged / pairingComplete / visibilitychange / Service-tab open (acceptance #3); `_setSummonGestureChoice()` is optimistic (move radio → `Flic.setSummonGesture` → reconcile to the plugin's echo → quiet-revert on failure). No new native code; gesture filter (T008) + summon path untouched; dev-panel toggle left in place (removed at T021). `node --check` clean. Files: `www/index.html`, `www/app.js`, `www/style.css` (one `.method-foot` rule). **On-device (owner):** switch to hold → next press needs a hold; switch back → single press summons — folds into the gesture-persistence / T016 reboot run-sheet.
- [x] T025 [APP] [US6] *(added 2 Jul — full end-to-end service test)* Register the **`buttonSelfTest`** listener in `www/app.js` → run the **service test**, **NOT** `_startHelpSequence`: fire an **immediate local ack** (orb pulse + haptic) while it POSTs to **`/service-test`** (T027); on OK → **multi-channel confirmation** (chime T033 + orb ping rings + in-app ping + haptic — "your button's working") and stamp the status row (T017) "last confirmed working: <time>"; on **timeout → honest "couldn't confirm"** and record a **no-response** (FR-028). Render the **fifth mockup state** (working / couldn't-confirm) bound to live night tokens — NOT mockup hex. Never opens the cancel window, never contacts anyone. Re-register on `resume`. (depends T008, T017, T027, T033) — ✓ `node --check`. **✅ BUILT 2026-07-03.** Real `buttonSelfTest` listener registered in `_initFlicListeners` (alongside `buttonSummon`, re-registered on resume) → the SAME `runServiceTest('physical_button')` helper as T029 — NEVER `_startHelpSequence`. Two-layer suppression intact: distinct gesture (`buttonSelfTest` ≠ `buttonSummon`) + distinct endpoint (`/service-test`, a leaf). `node --check` clean. **On-device double-tap PASS/NO-RESPONSE + suppression = T026** (owner + physical Flic).
- [~] T033 [APP] [US6] *(added 2 Jul — audio asset)* Bundle a **calm confirmation chime** in `www/` assets for the successful service test — **soft and reassuring, never a medical-alarm/beep timbre** (Constitution II / I.5); played by T025 on a pass (and only on a pass). **Chime asset is owner-SUPPLIED** (synthesised, no licensing) — bundle the supplied file into `www/` assets, **convert to a web-friendly format if needed**; tentative pick **Chime 2 (warm rise C→G)**, owner confirms on the Pixel speaker. **Asset RECEIVED + BUNDLED 2026-07-03** — owner supplied `iona_success_chime` (mp3 / ogg / wav in repo root); staged `iona_success_chime.mp3` (12.8KB, 64kbps mono, 48kHz) + `.ogg` fallback into `www/` (wav skipped — 618KB, too big to bundle). — ✓ file loads · DoD: chosen chime plays on the Pixel with a passing test — **PENDING:** the play wiring is T025 (service-test confirmation), not yet built; on-device speaker confirmation owner-driven.
- [x] T026 [NATIVE+APP] [US6] *(added 2 Jul)* **On-device service-test proof:** double-tap → multi-channel confirmation within ~2 s, status timestamp updates, **a "Service Test — Passed" row lands**, with **0** help sequences and **0** contacts reached [SC-008]; then **stop the listener (kill FGS / sleep) → double-tap → honest "couldn't confirm" + a "Service Test — No Response" row** [SC-009] — proving it surfaces silent death, not hides it (ties T024). **Suppression checks:** (i) a test never opens the cancel window / never dispatches; (ii) **with summon = short-press, rapid/tremor double presses and holds all SUMMON — none trigger the test** (FR-026a), while the in-app "Test service" still works. (depends T025) — DoD: safety.md CHK024–CHK029a pass. **PARTIAL 2026-07-03 — deployed + backend-verified; on-device proof owner-driven (needs the physical Flic double-tap).** Backend HTTP-verified: `/service-test` → Passed, `/service-test outcome=no_response` → No Response (both 200 + logged). App deployed. **Remaining (owner):** double-tap → chime + "working" card + `Service Test — Passed` row + **0 cancel windows / 0 contacts** (SC-008); listener-stopped/offline → double-tap → "couldn't confirm" + `Service Test — No Response` row (SC-009). Suppression holds by construction — `runServiceTest` shares no code with `_startHelpSequence`. **✅ ON-DEVICE PASS 2026-07-04 (SC-008/009):** in-app "Test service" → `Service Test — Passed` row + **0 dispatch** (log-confirmed, no escalation/econtact/cancel-window); airplane-mode → honest "couldn't confirm" card.
- [x] T027 [BE] [US6] *(added 2 Jul)* In `reply_to_airtable_webhook.py`, stand up a **permanent `POST /service-test`** endpoint: identify the member (same recId scheme as existing endpoints — auth hardening deferred per project norm), **log the test event via `event_logger`**, and **return OK — MUST NOT trigger any reminder, escalation, or contact** (suppression, FR-026). Restart webhook. (depends T028) — ✓ `python3 -m py_compile` + `grep -c 'ff"'` = 0 · DoD: `/service-test` returns 200, a row logs, **and no dispatch fires** (verified against a test member). **✅ DONE 2026-07-03** — webhook v2.2→v2.3; `_handle_service_test` leaf + do_POST branch (200 on ok / 400 on missing recId) + OPTIONS allow-list; py_compile clean, `ff"`=0. **Live localhost HTTP verified:** valid→200 `{ok,passed}` + row logged (deleted), missing→400 `{no recId, no log}`, OPTIONS→200. Suppression by construction (no run_escalation / FCM / contact). **Not yet restarted on the live server** (started only for the test, then stopped) — comes up with `run_servers.sh`. On-device T026 pending.
- [x] T028 [BE] [US6] *(added 2 Jul)* Register the new **EventLog Type/Status** in `skills/event_logger/scripts/event_logger.py` `VALID_*` sets — **"Service Test"** (Status `Passed` / `No Response`) and **"Button Battery Low"** — and add them to master-ref §5 (the known recipe; free-text fields, **no Airtable schema change**). **Register before first log** or the event is silently dropped (the PWA-pause pitfall). — ✓ `py_compile` · DoD: a live test row lands in EventLog. **✅ DONE 2026-07-03** — event_logger.py v2.7→v2.8; 2 Types (`Service Test`, `Button Battery Low`) + 3 Statuses registered; py_compile clean; live smoke test (`Service Test — Passed`) accepted + row auto-deleted. **Master-ref §5 doc registration still pending** (deferred to the Feature-005 /howsu-align).
- [x] T029 [APP] [US6] *(added 2 Jul)* Add the in-app **"Test service"** control to the Service-tab status row (`www/index.html` + `www/style.css` + `www/app.js`) → runs the **same** `/service-test` path (T027) minus the BLE hop, same confirmation + logging. One calm line framing it as reassurance. (depends T017, T027) — ✓ `node --check` · DoD on Pixel: tapping it logs a passing test and confirms. **✅ BUILT 2026-07-03.** Shared **`runServiceTest(source)`** helper (one impl, two entry points): POST `/service-test` with recId + source, 6s timeout (`SERVICE_TEST_TIMEOUT_MS`). PASS → `_playConfirmChime` (T033) + the **"working" card** (reuses the T012 `test-working` state) + last-confirmed stamped. NO RESPONSE → no chime + the **"couldn't confirm" card** (`test-failed` state) + a best-effort `outcome:"no_response"` follow-up → logs **Service Test — No Response** (FR-028). Status-row Test button → `runServiceTest('in_app')`. **Backend HTTP-verified** (both outcomes 200 + logged + cleaned up); `node --check` clean. On-device tap-to-confirm rides T026.
- [~] T030 [NATIVE] [US5] *(added 2 Jul — low battery)* In `FlicPlugin`, wire the SDK `Flic2ButtonListener` **battery callback** → expose `readBattery()` and emit a **`batteryLevel`** event; define a **low threshold** below which the app is told to warn. (depends T005) — ✓ `assembleDebug`. **RECONCILE 2026-07-03 — native side already built + compiled:** FlicPlugin carries `readBattery()` + the `batteryLevel` emit (plus T013's `isIgnoringBatteryOptimizations` / `requestBatteryExemption` helpers). **✅ RESOLVED 2026-07-04 (T031):** the low threshold is defined app-side (`FLIC_BATT_LOW_PCT = 25`) and the `batteryLevel` listener is now wired to `_handleBatteryLevel` (calm heads-up + carer-visible log), no longer a dev-panel stub. Native emit unchanged. On-device exercise remains T032.
- [x] T031 [APP] [US5] *(added 2 Jul)* On `batteryLevel` below threshold → a **calm early heads-up** ("your button's battery is getting low — worth changing it soon", **never** "failure"/alarm — FR-024), a **status-row battery indicator** (T017), and a logged **"Button Battery Low"** event via `/service-test`-style call or the existing log path (T028). (depends T017, T028, T030) — ✓ `node --check`. **✅ DONE 2026-07-04.** The status-row indicator + calm copy already existed (T017: `battLow` at 25%, amber `--signal-watch`, re-renders on `batteryLevel`); this added the PROACTIVE half. Threshold lives in **`FLIC_BATT_LOW_PCT = 25`** (`www/app.js`, shared by the status row + heads-up). New `_handleBatteryLevel(pct)` on the `batteryLevel` listener fires **once per low episode** (`_battLowNotified` flag + hysteresis at `+5%`): a calm `_showCalmNote('Your button’s battery is getting low — worth changing it soon.')` + a best-effort `_logButtonBatteryLow()` → **new leaf `POST /button-battery-low`** (`reply_to_airtable_webhook.py`, mirrors `/service-test`; logs `Button Battery Low`/`Button Battery Low`; no dispatch, shares no summon/service-test path). Copy banned-list clean, pronoun-free, calm amber (never alarm-red). **Backend live-verified 04 Jul:** PASS→`200 {ok:true}` + a `Button Battery Low` row; missing recId→`400`, nothing logged; no dispatch. `node --check` + `py_compile` (`ff"`=0) clean. On-device drive/simulate = T032.
- [x] T032 [NATIVE+APP] [US5] *(added 2 Jul)* **On-device:** drive a low-battery state (or simulate the callback) → confirm the calm heads-up + status indicator appear and a **"Button Battery Low"** row logs — **before** any loss of function. (depends T031) — DoD: SC-010 observed; safety.md CHK030/CHK031 pass. **Build ready 2026-07-04** — T031 wired + backend live-verified; run-sheet delivered. **✅ ON-DEVICE PASS 2026-07-04 (SC-010):** FLIC DEV → Sim low batt → calm note + amber status line + `Button Battery Low` row (log-confirmed `[BATTERY-LOW] … pct=18%`), and the button still summons normally.

---

## Phase 6 — Polish & cross-cutting

- [x] T020 [APP+NATIVE] Vocabulary + copy sweep of ALL new strings (pairing flow, status row, notification): notification is exactly **"Iona is here for you."**; no "watching"/"check-in"/care/welfare/alarm words; pressing for help never reads as "off"; Iona pronoun-free (FR-023/FR-024, Constitution II/I.6). — DoD: safety.md CHK020/CHK021 pass. **✅ DONE 2026-07-04** — swept all new/changed member-facing strings (notification, boot nudge, gesture chooser, battery heads-up, service-test cards, status row): 0 banned terms, pronoun-free, name-led, not alarm-styled, "pressing for help never reads as off". Notification `"Iona is here for you."` live-verified on device (`android.title`).
- [ ] T021 [BE] **Cross-repo cleanup — AFTER native is verified:** remove the **throwaway** `/flic-test` bring-up endpoint (the 3 comment-marked blocks) from `/Users/Henry/.openclaw/workspace/howsu/reply_to_airtable_webhook.py` and retire the old Flic-app Internet Request actions. **NB:** this removes only the *scaffolding* — the permanent **`/service-test`** endpoint (T027) stays; do not delete it. Restart the webhook. — ✓ `python3 -m py_compile` + `grep -c 'ff"'` = 0 · DoD: `/flic-test` returns 404; `/service-test` still 200; old rig retired. **⏸ DEFERRED by owner 2026-07-04** — retain the **FLIC DEV panel** (`_initFlicDevPanel` + the `__bsFakeBatt` status override + dev listeners in `www/app.js`) AND the `/flic-test` scaffolding for now; they may help diagnose any field issues and can be removed at a later date. Both are inert to members (the panel is a hidden dev tool; `/flic-test` is an unused extra endpoint) — the feature works with them present. `/service-test` + `/button-battery-low` stay regardless.
- [x] T022 **Post-bring-up Constitution re-check** on device: I.4 (stale + duplicate), I.6 (notification pronoun-free), II (no banned copy), I.2 (summon path unchanged/no parallel path). — DoD: plan.md re-check section satisfied. **✅ PASSED 2026-07-04** — I.2 (same `_startHelpSequence`, no parallel path; 2b re-confirmed after gating), I.4 (120s stale drops loudly, duplicate absorbed, press-time recovery heals, unlock-once gap signed off as accepted/visible via the nudge), I.6 (notification "Iona is here for you." live; pronoun-free), II (T020 sweep, 0 banned terms), service-test suppression (0 dispatch), deferred-scaffolding safety (**dev panel gated behind `dev_mode`, default off — ⚙ chip confirmed gone on-device**; `/flic-test` logs-only), FR-023a gating (button-less = no service/notification code-verified; paired = 2b arms). **Feature 005 COMPLETE.**

---

## Dependencies & execution order

- **PRE-1** gates the pairing UI (T012–T014). **PRE-2** gates T002.
- **Phase 1 (T001–T006)** is foundational — blocks everything native. Order: T001→T002 (gradle) ‖ T003 (manifest), then T004→T005→T006.
- **Phase 2** is the SAFETY MVP: T007/T008 (native) → T009 (app wire) → T010 (core proof) → T011 (safety proof). Prove this on hardware before building the pairing polish.
- **Phase 3 (pairing UI)** needs PRE-1 + the plugin (T005) — can begin in parallel with Phase 2 native work, but T014 verifies after the plugin exists.
- **Phase 4 (durability)** builds on T006's reconnect; verify after Phase 2. **T023 (BootReceiver)** makes SC-004's reboot leg real and is verified by **T016**; **T024 (OEM survival)** rides the T013 battery-opt exemption and is the S22 gate.
- **Phase 5** depends on the plugin commands (T005/T008). The **service test (T025/T026)** rides the `buttonSelfTest` event (T008), the status row (T017), **and the BE endpoint + EventLog types (T027 ← T028)** — stand those up before T025's on-device. **Low battery (T030→T031→T032)** rides `readBattery`/`batteryLevel` (T005/T030) + the EventLog type (T028).
- **T028 (EventLog types)** is the first BE step — register before anything logs, or events are silently dropped. **T027 (/service-test)** depends on it.
- **T021 (rig removal)** runs LAST — only once the native path is verified on device; it removes only the throwaway `/flic-test` scaffolding, **never** the permanent `/service-test` (T027).

## Parallel opportunities

- `[P]` T003 (manifest) can proceed alongside T001/T002 (gradle) — different files.
- Phase 3 UI markup (T012) can be built while Phase 2 native guards (T007/T008) are in progress — different repos/files — but each story's on-device DoD verifies in order.

## Implementation strategy

1. **PRE-1/PRE-2** (owner + version pin).
2. **Native foundation** (Phase 1) → scaffold builds, plugin reachable, listening service + "Iona is here for you." live.
3. **Safety MVP** (Phase 2) — the button starts the REAL help sequence; stale dropped; duplicate absorbed. Prove on the Pixel first (I.4).
4. **Pairing flow** (Phase 3) once the mockup lands, then **durability + reboot re-arm** (Phase 4).
5. **Status/remove/gesture + the reassurance layer** (Phase 5): stand up the EventLog types (T028) → `/service-test` endpoint (T027) → the double-tap **end-to-end service test** (T025/T026, with suppression proof) and the "Test service" control (T029); then **low-battery** surfacing (T030–T032).
6. **Polish + retire the throwaway rig** (Phase 6, keeping `/service-test`), then the on-device constitution re-check.

## Notes

- **No new help path.** T009 wires into the existing `_startHelpSequence`; the cancel window and duplicate-absorption guard are inherited and verified (T011), never rebuilt.
- **Native/app files differ by repo area** — same-file native tasks (FlicPlugin: T005/T007/T008) are sequential, not `[P]`.
- **The stale-press guard (T007) and reconnect-on-launch (T006/T015) are the two silent-failure risks** — each has an explicit on-device gate (T011, T016).
- **Silent-death defences (added 2 Jul).** Four anti-silent-death measures now sit together: **(1) reboot** — a `BootReceiver` (T023) re-arms the FGS on cold boot, since SDK storage persists only the pairing, not a listener; **(2) OEM kill** — a battery-optimization exemption in pairing (T013) + the S22 survival gate (T024); **(3) low button battery** — an early calm heads-up + status indicator + logged event (T030–T032), the commonest real cause of a dead button; **(4) the user-facing check** — the **double-tap service test (T025–T029)**: a **full end-to-end** round-trip (button → app → `/service-test` → EventLog) that contacts no one, logs **both** a pass and a no-response, so if anything upstream was killed the double-tap returns an honest "couldn't confirm" (a recorded fact) instead of silence. Where an OEM still wins, that limit is **surfaced by the service test**, never hidden.
- **Suppression is safety-critical (FR-026).** The service test never dispatches — guarded at two layers: a distinct gesture (`buttonSelfTest` ≠ `buttonSummon`) **and** a distinct endpoint (`/service-test` ≠ the summon path). Verified on device (T026 / safety.md §I).
- **Status (reconciled 2026-07-03 to verified on-disk + on-device truth):** Phase 1 (T001–T006; **T003 partial** — boot-survival manifest decls deferred to T013/T023) + Phase 2 (T007–T011, T034) + the **press-time-recovery hardening (T035)** are BUILT and on-device-verified on the Pixel 4a. **T030 partial** — native battery emit built, app-side + on-device legs remain. **T027/T028** (service-test backend) BUILT + HTTP-verified on 2026-07-03. **T012** pairing UI BUILT + on-device-render-verified (Pixel 4a) 2026-07-03; **T033** chime asset bundled. **PRE-1** mockup built (owner commit pending). Phases 3–6: 7 done (T012, T013, T017, T025, T027, T028, T029), 3 partial (T026, T030, T033), 12 not-started. The original "nothing built yet — awaiting approval" note is **superseded**.
- **Legend for reconciled marks:** `[x]` done + on-device-verified · `[~]` partial (see the task's inline RECONCILE note) · `[ ]` not started.
